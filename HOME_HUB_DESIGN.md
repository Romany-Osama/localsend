# Home Hub — التصميم المعتمد

## الهدف

إضافة طبقة منزلية فوق LocalSend تجعل التطبيق مركزًا محليًا للجروبات، الشات، الإرسال الجماعي، الإشعارات، واستئناف النقل، مع إبقاء كل وظائف LocalSend الأصلية دون تغيير. لا توجد حسابات سحابية أو أرقام هاتف أو خادم إنترنت.

## القيود الأساسية

| القاعدة | القرار |
|---|---|
| الشبكة | LAN/Wi‑Fi مباشر فقط للميزات الجديدة |
| خادم دائم | غير مطلوب؛ لا توجد مكتبة منزلية تحتاج جهازًا يعمل طوال الوقت |
| وجود الجهاز | الرسائل والطلبات تصل فقط عندما يكون الجهاز متصلًا ومتاحًا |
| العضوية | كل جهاز يوافق بنفسه على دعوة الجروب |
| إرسال لجروب | كل مستلم يوافق أو يرفض بشكل مستقل |
| الصلاحيات | صلاحية مشاهدة/إرسال/إدارة، مع انتهاء تلقائي لوضع الضيف |
| التخزين | كل جهاز يحتفظ بسجل محلي خاص به؛ لا يوجد مخزن مركزي |
| الوظائف الأصلية | تبقى منفصلة ولا تتغير بروتوكولاتها |
| المكالمات | طبقة اختيارية لاحقة، signaling محلي فقط، ولا تُفعّل تلقائيًا |

## المكونات

### الهوية والأجهزة

كل نسخة تنشئ `device_id` عشوائيًا مرة واحدة وتحتفظ به محليًا مع اسم الجهاز ونوعه. اكتشاف الأجهزة يعيد استخدام LocalSend discovery الحالي. لا يتم إنشاء حساب مركزي، ولا تُرسل الهوية إلى خدمة خارجية.

### الجروبات

ينشئ جهاز ما `group_id` واسم الجروب وسرًا محليًا للجروب. الدعوة تنتقل مباشرة إلى الجهاز الهدف، ويجب أن يوافق الهدف قبل إضافة العضوية. يحتفظ كل جهاز بنسخة من عضويته وأدواره. يستطيع مدير الجروب إلغاء عضو، لكن لا يستطيع تجاوز موافقة العضو على الرسائل أو الملفات.

### الشات

كل رسالة لها `message_id` و`group_id` و`sender_id` ووقت محلي ونسخة protocol. يرسل المرسل الرسالة مباشرة إلى الأجهزة المتصلة في الجروب، ويحتفظ كل جهاز برسائله المستلمة محليًا. إذا كان عضو غير متصل، تُحفظ الرسالة في outbox محلي للمرسل وتُعاد عند ظهوره. لا توجد وعود بتاريخ مركزي كامل عندما يكون المرسل الأصلي غير متصل.

### الإرسال الجماعي

ينشئ المرسل `broadcast_id` وعرضًا يتضمن أسماء الملفات وأحجامها وhash اختياري. يرسل العرض مباشرة إلى كل أعضاء الجروب. كل جهاز يعرض طلب موافقة مستقلًا. بعد القبول يبدأ نقل مباشر إلى ذلك الجهاز فقط، وتبقى الحالات منفصلة: `pending`, `accepted`, `rejected`, `offline`, `transferring`, `completed`, `failed`.

### البث الجماعي

يمكن استخدام Stream & Browse لبث ملف واحد إلى أكثر من جهاز، لكن كل جهاز يحتاج grant وقرارًا منفصلًا. لا يُنسخ الملف إلى الجهاز الطالب إلا إذا اختار المستخدم تنزيله صراحةً. ينتهي grant عند الإلغاء أو انتهاء TTL أو إغلاق الجلسة.

### وضع الضيف

الدعوة المؤقتة تحدد الجروب والعمليات المسموحة ووقت الانتهاء. الضيف لا يرى الأجهزة أو الملفات خارج النطاق الممنوح له. زر `Revoke all` يلغي كل grants والدعوات والجلسات الخاصة بالجهاز فورًا.

### الإشعارات والاستئناف

الإصدار الأول يستخدم إشعارات داخل التطبيق ثم إشعارات نظام محلية عند توفر دعم المنصة. كل عملية نقل تملك حالة وoffset وhash للجزء المنقول، حتى يمكن استئنافها بعد انقطاع Wi‑Fi بدل البدء من الصفر.

### المكالمات الصوتية

تُنفذ لاحقًا كميزة اختيارية خلف capability flag. الـ signaling يتم عبر HTTP محلي داخل الشبكة، والصوت يمر peer-to-peer داخل LAN. لا يتم استخدام push service أو STUN/TURN خارجي افتراضيًا. إذا لم يدعم plugin منصة معينة، تظل الجروبات والشات والنقل متاحة دون تعطيلها.

## مسارات HTTP المقترحة

| المسار | الغرض |
|---|---|
| `/home-hub/v1/device` | تعريف الجهاز والقدرات المحلية |
| `/home-hub/v1/groups/invite` | إرسال دعوة جروب |
| `/home-hub/v1/groups/invite/respond` | قبول أو رفض الدعوة |
| `/home-hub/v1/groups/events` | رسالة أو presence أو تغيير عضوية |
| `/home-hub/v1/transfers/offer` | عرض إرسال جماعي أو attachment |
| `/home-hub/v1/transfers/respond` | قرار المستلم الفردي |
| `/home-hub/v1/transfers/stream` | نقل أو استئناف body بعد grant |
| `/home-hub/v1/sessions/revoke` | إلغاء الجلسات والمنح |
| `/home-hub/v1/calls/offer` | signaling محلي للمكالمة لاحقًا |

## مراحل التنفيذ

1. نماذج الهوية والجروب والدعوة والصلاحيات داخل `packages/core` مع تخزين محلي في Flutter.
2. واجهة الجروبات والدعوات وقائمة الأجهزة المتصلة.
3. الشات المباشر وسجل الرسائل المحلي.
4. الإرسال الجماعي بعروض وموافقة منفصلة وحالات لكل جهاز.
5. الاستئناف والإشعارات ووضع الضيف والإلغاء الشامل.
6. دراسة plugin صوتي متعدد المنصات، ثم إضافة المكالمات كطبقة اختيارية واختبار LAN-only.
7. بناء Android وWindows وLinux وmacOS وiOS unsigned وتحديث Release.
