# Stream & Browse for LocalSend

هذا الفورك يحافظ على بروتوكولات LocalSend ومسارات الإرسال والاستقبال الأصلية، ويضيف وضعًا اختياريًا باسم **Stream & Browse** للعمل داخل الشبكة المحلية.

## السلوك

يختار صاحب الجهاز مجلدات مشاركة محددة من تبويب Stream. الجهاز البعيد يفتح جلسة تصفح، لكن الجلسة لا تصبح فعالة إلا بعد موافقة الجهاز الذي يحتوي على الملفات. بعد ذلك يستطيع الجهاز الطالب رؤية أسماء الجذور والمجلدات والملفات فقط. عند اختيار ملف، يُرسل طلب موافقة ثانٍ خاص بهذا الملف. لا يمنح قبول جلسة التصفح وحده صلاحية قراءة أي ملف.

بعد قبول الملف، يُصدر الخادم تصريحًا مؤقتًا للقراءة فقط. المشغل يقرأ من endpoint البث مباشرة مع دعم HTTP Range و`206 Partial Content`، لذلك يمكن التقديم والتأخير دون نسخ الملف كاملًا إلى الجهاز الطالب. يبقى الملف في مكانه على الجهاز المصدر، ويجب أن يظل الجهازان على نفس الشبكة وأن يبقى الجهاز المصدر يعمل.

## المسارات الجديدة

| Method | Path | الغرض |
| --- | --- | --- |
| `POST` | `/api/localsend/stream/v1/session` | طلب جلسة تصفح، وتتطلب موافقة صاحب الجهاز |
| `GET` | `/api/localsend/stream/v1/roots?sessionId=...` | عرض الجذور المشتركة بعد قبول الجلسة |
| `GET` | `/api/localsend/stream/v1/entries?...` | عرض محتويات مجلد محدد |
| `POST` | `/api/localsend/stream/v1/file-request` | طلب تصريح قراءة لملف واحد |
| `GET` | `/api/localsend/stream/v1/stream?...` | بث الملف المصرّح به مع Range |
| `POST` | `/api/localsend/stream/v1/session/revoke?...` | إلغاء الجلسة وإبطال التصاريح |

## الأمان

المشاركة قراءة فقط، والجذور تختار يدويًا، والمسارات النسبية تُطبّع وتُرفض فيها مكونات `..`، ولا تُتبع الروابط الرمزية أثناء التصفح. التصاريح مؤقتة، ومربوطة بعنوان IP الخاص بالعميل وبجلسة محددة. مع ذلك، هذه النسخة مصممة للشبكة المحلية الموثوقة؛ لا ينبغي فتح منفذ Stream مباشرة على الإنترنت قبل إضافة مصادقة أقوى وتثبيت الشهادات في عميل Stream.

## تشغيل Flutter

بيئة التشغيل يجب أن تحتوي على Flutter/Dart المطابقين للمشروع. بعد ذلك شغّل `flutter pub get` من جذر المشروع، ثم نفّذ البناء المعتاد للمنصة المطلوبة. أضيفت حزم `media_kit` و`media_kit_video` و`media_kit_libs_video` لتشغيل روابط الفيديو الشبكية داخل واجهة الموبايل والكمبيوتر، مع تهيئة `MediaKit` في نقطة تشغيل التطبيق.

في هذه البيئة تم فحص طبقة Rust واختبارات Stream، لكن لم يتم تشغيل `flutter analyze` أو `flutter build` لأن Flutter SDK غير مثبت داخل الـ sandbox. لذلك يجب إجراء اختبار بناء حقيقي لكل منصة بعد جلب الاعتمادات.

## التطوير اللاحق المقترح

الخطوة التالية المهمة هي حفظ جذور المشاركة في الإعدادات مع إعادة طلب صلاحيات النظام عند الحاجة، وإضافة دعم TLS/pinning إلى عميل Stream مثل طبقة LocalSend الحالية. بعد ذلك يمكن إضافة playlist، subtitles، اختيار مسارات الصوت، وتنزيل اختياري لجزء من الملف للمشاهدة دون اتصال.
